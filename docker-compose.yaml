version: '3'

services:
  web:
    build: .
    container_name: mimiw-portfolio
    ports:
      - "8080:80"
    # 👇 เพิ่มส่วนนี้เข้ามาครับ
    volumes:
      - .:/usr/share/nginx/html